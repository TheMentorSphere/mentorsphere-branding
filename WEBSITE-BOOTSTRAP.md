# Website rebuild installation

This temporary commit contains a checksum-verified package of the complete website rebuild. The included GitHub Actions workflow extracts the production-ready site into `docs/`, removes the temporary package and commits the finished files back to this branch.

The live website and DNS are not changed by this branch.